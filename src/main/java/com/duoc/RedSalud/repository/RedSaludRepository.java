package com.duoc.Redsalud.repository;

import com.duoc.Redsalud.model.RedSalud;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RedSaludRepository extends JpaRepository <RedSalud, Long> {
}
